{
  "entities": {
    "Roadmap": {
      "title": "Roadmap",
      "description": "An autonomous career navigation roadmap generated by Mentra",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique roadmap document ID"
        },
        "userId": {
          "type": "string",
          "description": "User UID who owns this roadmap"
        },
        "targetRole": {
          "type": "string",
          "description": "The target job role entered by the user",
          "maxLength": 200
        },
        "resumeSkills": {
          "type": "string",
          "description": "The current skills or resume excerpt provided by the user",
          "maxLength": 10000
        },
        "resume_match_score": {
          "type": "number",
          "description": "Resume match score between 0 and 100"
        },
        "critical_skill_gaps": {
          "type": "array",
          "description": "Array of 3 critical technical skill gaps"
        },
        "4_week_roadmap": {
          "type": "object",
          "description": "Structured 4-week career navigation roadmap"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp"
        }
      },
      "required": ["userId", "targetRole", "resume_match_score", "critical_skill_gaps", "4_week_roadmap"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A chat interaction message with the AI Career Mentor for a specific roadmap",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique chat message ID"
        },
        "userId": {
          "type": "string",
          "description": "User UID who owns this conversation"
        },
        "role": {
          "type": "string",
          "description": "Message author: user or model"
        },
        "content": {
          "type": "string",
          "description": "The textual content of the message",
          "maxLength": 10000
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Message creation timestamp"
        }
      },
      "required": ["userId", "role", "content"]
    }
  },
  "firestore": {
    "/roadmaps/{roadmapId}": {
      "schema": "Roadmap",
      "description": "Collection of roadmaps strictly isolated by user UID"
    },
    "/roadmaps/{roadmapId}/chats/{chatId}": {
      "schema": "ChatMessage",
      "description": "Sub-collection of multi-turn conversation messages with the AI Mentor for this roadmap"
    }
  }
}
